Перейти к содержанию
Меню навигации
сплошной смех

Код
Проблемы
Развертывание статического контента на страницах
Удалить каталог .github/index.html/<head>/< #29
Файл рабочего процесса для этого запуска
.github/workflows/static.yml по адресу 3f5d956
# Простой рабочий процесс для развертывания статического контента на страницах GitHub
имя : Развертывание статического контента на страницах

на :
  # Запускается при отправке изменений, нацеленных на ветку по умолчанию
  толкать :
    ветви : ["main"]

  # Позволяет запустить этот рабочий процесс вручную из вкладки «Действия»
  workflow_dispatch :

# Устанавливает разрешения GITHUB_TOKEN, чтобы разрешить развертывание на страницах GitHub
разрешения :
  содержание : читать
  страницы : писать
  id-токен : запись

# Разрешить только одно одновременное развертывание, пропуская запуски, находящиеся в очереди между текущим запуском и последним поставленным в очередь.
# Однако НЕ отменяйте текущие запуски, так как мы хотим, чтобы эти производственные развертывания были завершены.
параллелизм :
  группа : " страницы "
  отмена в процессе : ложь

вакансии :
  # Единичное задание по развертыванию, поскольку мы просто развертываем
  развертывать :
    среда :
      имя : github-pages
      URL-адрес : ${{ steps.deployment.outputs.page_url }}
    запускается на : ubuntu-latest
    шаги :
      - имя : Оформить заказ
        использование : actions/checkout@v4
      - имя : Страницы настройки
        использует : actions/configure-pages@v5
      - имя : Загрузить артефакт
        использует : actions/upload-pages-artifact@v3
        с :
          # Загрузить весь репозиторий
          путь : ' . '
      - имя : Развернуть на страницах GitHub
        идентификатор : развертывание
        использует : actions/deploy-pages@v4
Удалить каталог .github/index.html/<head>/< · ivanm696/solid-giggle@3f5d956
