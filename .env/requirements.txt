# ---------------------------
#  solid-giggle/requirements.txt
# ---------------------------

# Библиотека Telegram Bot API (объектно‑ориентированная)
python-telegram-bot==20.3

# Асинхронные HTTP‑запросы – удобно для внешних API
aiohttp==3.9.5

# Лёгкая работа с переменными окружения (dotenv‑файл)
python-dotenv==1.0.1

# Встроенное SQLite‑хранилище для простых задач
# (устанавливается вместе с Python, но указываем явно для ясности)
sqlite3

# Логирование (Python‑стандартная библиотека, но добавляем для удобства)
# (если хотите более гибкое логирование – можно добавить loguru)
# loguru==0.7.2

# (Опционально) Если вам нужно парсить JSON‑ответы быстрее
# ujson==5.9.0

# ---------------------------
#  Как это использовать
# ---------------------------

# 1. Создайте виртуальное окружение
#    python -m venv venv
#    source venv/bin/activate      # Linux/macOS
#    .\venv\Scripts\activate       # Windows

# 2. Установите зависимости
#    pip install -r requirements.txt

# 3. Запустите бота
#    python bot.py
