Меню
Список ошибок

Задайте вопрос искусственному интеллекту об этой странице.
Последнее обновление: 25 ноября 2025 г.
Отсутствует публичный каталог
Этап сборки завершится ошибкой, если выходной каталог отсутствует, пуст или недействителен (например, не является каталогом). Для устранения этой ошибки можно попробовать следующие действия:

Убедитесь, что выходной каталог правильно указан в настройках проекта.
Если выходной каталог указан правильно, проверьте команду сборки ( документация ) или корневой каталог .
Попробуйте запустить команду сборки локально и убедитесь, что файлы корректно создаются в указанном выходном каталоге.
Отсутствует скрипт сборки.
Это актуально только при использовании Vercel CLI версии 16.7.3 или более старой.
Предположим, ваш проект содержит package.jsonфайл, но не apiимеет директории и vercel.jsonконфигурации. В этом случае ожидается, что он предоставит build скрипт , который выполнит статическую сборку вашего фронтенда и выведет её в publicдиректорию в корневом каталоге вашего проекта.

При правильной настройке ваш package.jsonфайл будет выглядеть примерно так:

package.json
{
  "scripts": {
    "build": "[my-framework] build --output public"
  }
}
Пример buildскрипта в package.jsonфайле, указывающий выходной каталог.

После определения build скрипта эта ошибка исчезнет. Более того, она не будет отображаться, если вы используете его package.jsonисключительно для предоставления зависимостей для ваших функций Vercel, расположенных в этом apiкаталоге.

Максимальное количество запросов от членов команды
Максимальное количество открытых заявок на вступление в команду — 10. Для того чтобы принять больше заявок, существующие заявки должны быть одобрены или отклонены владельцем команды .

Это гарантирует, что список всегда останется управляемым и защищенным от спама.

Приглашение пользователей в команду, запросивших доступ.
Если пользователь уже запросил доступ к команде, пригласить его невозможно. Вместо этого, для получения доступа запрос должен быть одобрен владельцем команды .

Это гарантирует, что ни одно приглашение в команду не будет принято случайно.

Запросите доступ, используя необходимую учетную запись Git.
Если развертывание коммита завершается с ошибкой "Для развертывания требуется доступ к команде", это означает, что учетная запись Git автора коммита не подключена к команде Hobby, являющейся членом этой команды.

Ссылка, прикрепленная к сообщению об ошибке, позволяет пользователю подключить свою команду Hobby к учетной записи Git автора коммита. Если команда Hobby подключена к другой учетной записи Git, возникнет ошибка с сообщением о том, что для запроса доступа к команде необходимо использовать другую учетную запись Git.

После подключения учетной записи Git к команде Hobby на Vercel, можно запросить доступ к команде. Владелец команды может одобрить или отклонить этот запрос. Если запрос будет одобрен, неудачная попытка коммита будет повторена, и последующие коммиты не будут завершаться с ошибкой из-за отсутствия доступа к команде.

Заблокированные области видимости
Развертывание, проект, пользователь или команда на платформе Vercel могут быть заблокированы, если они нарушают наши правила добросовестного использования или Условия предоставления услуг .

Заблокированные развертывания и проекты приведут к ошибке 451 на вашем веб-сайте. Заблокированные пользователи и команды не могут создавать новые развертывания, а также не могут быть приглашены в команду. Если вам нужна помощь, пожалуйста, свяжитесь с нами по адресу registration@vercel.com .

Неиспользуемые параметры сборки и разработки
У проекта есть несколько настроек, которые можно найти на панели управления. Один из разделов, « Настройки сборки и разработки» , используется для изменения способа сборки проекта.

Однако параметры сборки и разработки применяются только к развертываниям без какой-либо конфигурации .

Если в конфигурации развертывания задан соответствующий buildsпараметр, параметры сборки и разработки игнорируются.

Неиспользуемая настройка региона функции Верселя
В панели управления проекта есть несколько настроек. Одна из них, « Регион выполнения функций Vercel» , используется для выбора региона , в котором выполняются ваши функции Vercel.

Если regionsв конфигурации развертывания задан параметр "Vercel Function Region vercel.json", то параметр "Vercel Function Region" игнорируется.

Если параметр определен в конфигурации развертывания CLI --regions, настройка области функций Vercel игнорируется.

Неверный шаблон источника маршрута
Это sourceсвойство следует синтаксису от path-to-regexp , а не RegExpсинтаксису.

Например, отрицательные прогнозы должны быть объединены в группу.

До

vercel.json
{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "source": "/feedback/(?!general)",
  "destination": "/api/feedback/general"
}
После

vercel.json
{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "source": "/feedback/((?!general).*)",
  "destination": "/api/feedback/general"
}
Недопустимый сегмент назначения маршрута
Это sourceсвойство соответствует синтаксису преобразования пути в регулярное выражение .

Двоеточие ( :) обозначает начало именованного параметра сегмента .

Именованный параметр сегмента, определенный в destinationсвойстве, также должен быть определен в sourceсвойстве.

До

vercel.json
{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "source": "/feedback/:type",
  "destination": "/api/feedback/:id"
}
После

vercel.json
{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "source": "/feedback/:id",
  "destination": "/api/feedback/:id"
}
Не удалось установить зависимости сборщика.
При выполнении vercel buildкоманд могут возникать ошибки vercel dev, если команда была вызвана для установки сборщиков, определенных в вашем файле.npm installnpmvercel.json

npm installможет не сработать, если:

npmне установлен
Ваше интернет-соединение недоступно.
Конструктор, определенный в вашей конфигурации, не опубликован в реестре npm.
Дважды проверьте правильность названия и версии запрашиваемого конструктора.

Смешанные свойства маршрутизации
Если в вашем конфигурационномrewrites файле указаны , redirects, headers, cleanUrlsили , то параметр определен не может быть.trailingSlashroutes

Это необходимое ограничение, поскольку routesэто примитив нижнего уровня, содержащий все остальные типы. Следовательно, его нельзя безопасно объединить с новыми свойствами.

Примеры сравнения с новыми объектами недвижимости приведены в разделе « Маршруты обновления» .routes

Конфликтующие конфигурационные файлы
Для обеспечения обратной совместимости Vercel CLI использует два соглашения об именовании конфигурационных файлов (например vercel.json, и now.json). Оба соглашения поддерживаются, однако одновременно может быть определен только один . Vercel CLI выдаст сообщение об ошибке, если будут использоваться оба соглашения об именовании одновременно.

Эти конфликтующие ошибки конфигурации возникают, если:

Оба vercel.jsonфайла now.jsonприсутствуют в вашем проекте.
Решение: Удалите now.jsonфайл.
Обе .vercelдиректории .nowсуществуют в вашем проекте.
Решение: Удалите .nowдиректорию.
Оба .vercelignoreфайла .nowignoreсуществуют в вашем проекте.
Решение: Удалите .nowignoreфайл.
Переменные среды, начинающиеся с префикса, VERCEL_могут иметь конфликтующую переменную среды, начинающуюся с префикса NOW_.
Решение: Определите только VERCEL_переменную среды с префиксом.
Конфликтующие функции и конфигурация сборки
Существует два способа настройки функций Vercel в вашем проекте: функции или builds . Однако одновременно можно использовать только один из них — их нельзя использовать одновременно.

В большинстве случаев рекомендуется использовать это functionsсвойство, поскольку оно поддерживает больше функций, таких как:

Позволяет настроить объем памяти, предоставляемый функции Верселя.
Более надежный вариант, поскольку требует определенной версии пакета npm для данного runtimeсвойства.
По умолчанию поддерживаются «чистые URL-адреса», что означает, что функции Vercel автоматически доступны без указания расширения файла в URL-адресе.
Однако buildsподдержка этой функции будет продолжена в целях обеспечения обратной совместимости.

Неподдерживаемая конфигурация функций в Nextjs
При использовании Next.js в свойстве functions можно настроить только memoryпараметры и . Остальные параметры конфигурации Next.js обрабатывают автоматически.maxDuration

Развертывание функций Vercel в нескольких регионах
Функции Vercel можно развертывать в нескольких регионах. Эта функциональность доступна только для корпоративных команд .

В тарифном плане Pro это ограничение существовало с момента запуска текущей модели ценообразования, но было введено 10 июля 2020 года . Для проектов, созданных 10 июля 2020 года или позже, развертывание в нескольких регионах больше невозможно.

Чтобы выбрать ближайший к вам регион, ознакомьтесь с нашим руководством по выбору регионов развертывания для функций Vercel.

Несоответствующий шаблон функции
Для свойства functions используется шаблон glob для каждого ключа. Этот шаблон должен соответствовать исходным файлам функций Vercel, расположенным в указанном apiкаталоге.

Если вы используете Next.js, исходные файлы функций Vercel можно создать следующим образом:

pages/apiкаталог
src/pages/apiкаталог
pagesдиректория, в которой модуль экспортирует getServerSideProps
src/pagesдиректория, в которой модуль экспортирует getServerSideProps
Кроме того, если вы хотите использовать функцию Vercel, написанную не на Node.js, и в сочетании с Next.js, вы можете поместить её в каталог api(предоставляемый платформой), поскольку pages/api(предоставляемый Next.js) поддерживает только JavaScript.

Запрещено

vercel.json
{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "functions": {
    "users/**/*.js": {
      "maxDuration": 30
    }
  }
}
Допустимый

vercel.json
{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "functions": {
    "api/users/**/*.js": {
      "maxDuration": 30
    }
  }
}
Разрешено (Next.js)

vercel.json
{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "functions": {
    "pages/api/users/**/*.js": {
      "maxDuration": 30
    }
  }
}
Не удаётся загрузить настройки проекта.
Если конфигурация проекта .vercelпринадлежит команде, в которой вы не состоите, попытка развертывания проекта приведет к ошибке.

Это может произойти, если вы клонируете репозиторий Git, включающий этот .vercelкаталог, или если вы вошли в систему под неправильной учетной записью Vercel. Кроме того, проблемы с аутентификацией могут возникнуть, если вы не соблюдаете политику двухфакторной аутентификации вашей команды.

Для исправления удалите .vercelкаталог и повторно разверните проект, выполнив следующие команды.

В macOS и Linux:

rm -rf .vercel
версель
В Windows:

rmdir /s /q .vercel
версель
Проверка названия проекта
Названия проектов могут состоять не более чем из ста буквенно-цифровых символов нижнего регистра. Дефисы могут использоваться между словами в названии, но никогда в начале или конце.

Ограничение на количество подключений к репозиторию
Количество проектов Vercel, которые можно связать с одним и тем же репозиторием Git, ограничено в зависимости от вашего тарифного плана .

Если вы достигли лимита и хотите подключить к репозиторию новый проект, вам потребуется отключить существующий проект от того же репозитория Git.

Для увеличения этого лимита, пожалуйста, свяжитесь с нашим отделом продаж .

Проверка домена через командную строку
Для подтверждения вашего домена направьте его на сервер Vercel, настроив наши серверы имен или DNS-запись. Более подробную информацию о том, как это сделать для вашего домена, можно получить, выполнив команду `npm run server` vercel domains inspect <domain>, где <domain>`<домен, который вас интересует>` — это интересующий вас домен.

В качестве альтернативы, если вы уже добавили домен в проект, ознакомьтесь с разделом «Настройка домена» в документации по пользовательским доменам.

Уход из команды
Вы не можете покинуть команду, если являетесь последним оставшимся владельцем или последним подтвержденным участником . Чтобы покинуть команду, сначала назначьте другого подтвержденного участника владельцем команды .

Если вы остались единственным участником , вам следует удалить команду.

Список игнорирования по умолчанию в Git
При развертывании с использованием Vercel CLI ряд файлов будет автоматически игнорироваться по соображениям безопасности и производительности.

Однако эти файлы не игнорируются при развертывании, созданном с помощью Git , и вместо них выводится предупреждение. Это связано с тем, что .gitignoreопределяется, какие файлы следует игнорировать.

Если файл был намеренно добавлен в Git, предупреждение можно игнорировать.

Если файл был случайно добавлен в Git, вы можете удалить его, используя следующие команды:

Терминал
git rm file.txt                   # remove the file
echo 'file.txt' >> .gitignore     # append file to .gitignore
git add .gitignore                # stage the change
git commit -m "Removed file.txt"  # commit the change
git push                          # deploy the change
Установка приложения GitHub не найдена.
В некоторых случаях регистрация на GitHub завершается неудачей из-за несоответствий в базе данных GitHub.

Когда вы связали свою команду Hobby с учетной записью GitHub, приложение Vercel GitHub было установлено в вашей учетной записи GitHub, после чего GitHub уведомил Vercel об успешной установке приложения.

Однако Vercel не смог получить информацию об установке приложения с GitHub, из-за чего создавалось впечатление, будто приложение Vercel на GitHub никогда не устанавливалось.

Для решения этой проблемы подождите пару минут и попробуйте снова подключиться к GitHub. Если подключение по-прежнему не удается, обратитесь в службу поддержки GitHub , чтобы выяснить причину, по которой не удалось установить приложение Vercel GitHub .

Предварительная ветка используется в качестве производственной ветки.
Если вы настроили пользовательскую ветку Git для домена или переменной среды, она считается предварительным доменом и предварительной переменной среды. Поэтому настроенная для неё ветка Git считается предварительной веткой .

При настройке производственной ветки в параметрах проекта использовать ветку предварительного просмотра невозможно.

Если вы по-прежнему хотите использовать эту конкретную ветку Git в качестве рабочей, выполните следующие действия:

Привяжите затронутые домены к производственной среде (очистите определенную для них ветку Git).
Присвойте затронутые переменные среды производственной среде (очистите определенную для них ветку Git).
После этого вы можете использовать ту ветку Git, которую изначально хотели использовать в качестве рабочей ветки.

Потерян доступ к репозиторию Git
Для того чтобы Vercel мог развертывать коммиты в вашем репозитории Git, к нему должен быть подключен проект на Vercel.

Соединение прерывается, если репозиторий Git удален, заархивирован или если приложение Vercel было удалено из соответствующей учетной записи Git или организации Git. Убедитесь, что ни одно из этих условий не выполняется.

Кроме того, при использовании GitHub соединение также прерывается, если вы или член вашей команды изменяете права доступа к приложению Vercel GitHub, установленному в соответствующей личной учетной записи GitHub или организации GitHub.

Чтобы проверить права доступа приложения Vercel GitHub, установленного в вашей личной учетной записи GitHub, перейдите на страницу «Приложения» и выберите Vercel в разделе «Установленные приложения GitHub» . Вы увидите список репозиториев Git, к которым приложение GitHub имеет доступ. Убедитесь, что репозиторий Git, который вы хотите подключить к проекту Vercel, указан в этом списке.

Чтобы проверить права доступа приложения Vercel GitHub, установленного в вашей организации GitHub, выберите Vercel в разделе «Установленные приложения GitHub» в настройках организации. Вы увидите список репозиториев Git, к которым приложение GitHub имеет доступ. Убедитесь, что репозиторий Git, который вы хотите подключить к проекту Vercel, указан в этом списке.

Повторное развертывание в производственной среде невозможно.
Вы не сможете повторно развернуть рабочую версию, если существует более новая.

Причина в том, что повторное развертывание старой рабочей версии приведет к перезаписи самого последнего исходного кода, развернутого в рабочей среде.

Чтобы принудительно перезаписать текущую рабочую версию развертывания, выберите вместо этого вариант «Продвинуть» .

Удаление SSL-сертификата отклонено
Некоторые SSL-сертификаты, связанные с вашей командой или группой (например, сертификаты Wildcard SSL для тестовых доменов вашего проекта Vercel), автоматически генерируются платформой Vercel.

Поскольку эти SSL-сертификаты управляются платформой Vercel, их нельзя удалить вручную ни на панели управления Vercel, ни через интерфейс командной строки Vercel.

Пользовательские SSL-сертификаты можно загружать в команды, использующие тарифный план Enterprise , и удалять их можно вручную.

Ветка разработки используется в качестве ветки предварительного просмотра.
Ветка Git, настроенная с помощью поля " Производственная ветка" в параметрах проекта, считается веткой, содержащей код, предоставляемый вашим посетителям.

Если вы хотите присвоить доменную переменную или переменную окружения конкретной ветке Git, нет необходимости заполнять ее вручную.

По умолчанию, если для них не определена пользовательская ветка Git, домены уже назначены производственной ветке. То же самое относится и к переменным окружения: если для них не определена пользовательская ветка Git и в качестве среды выбрана Production , они уже назначены производственной ветке.

Если вы все же хотите использовать определенную ветку Git для домена или переменной окружения, это должна быть ветка предварительного просмотра .

Команда не найдена в vercel dev
Сообщение об ошибке «Команда не найдена» появляется, когда дочерний процесс, пытающийся создать команду, не установлен на вашем локальном компьютере. Для корректной работы vercel devнеобходимо установить соответствующую программу в вашу операционную систему .vercel dev

Например, вы можете увидеть ошибку "Команда не найдена: go", если пишете функцию Vercel на Go, но у вас не goустановлен исполняемый файл. В этом случае вам нужно сначала установить его go , а затем попробовать снова вызвать вашу функцию Vercel.

Рекурсивный вызов команд
Почему произошла эта ошибка
Для вашего проекта вы настроили один из следующих параметров:

Команда сборки, определенная в настройках проекта, вызываетvercel build
Команда разработки, определенная в настройках проекта, вызываетvercel dev
Поскольку команда сборки вызывается при vercel buildразвертывании, она не может вызывать vercel buildсаму себя, так как это привело бы к бесконечной рекурсии.

То же самое относится и к команде разработки: при локальной разработке vercel devона вызывает команду разработки, поэтому не может вызвать vercel devсаму себя.

Возможные способы решения проблемы
Настройте команды сборки и разработки, определенные для вашего проекта, таким образом, чтобы они не вызывали vercel buildили vercel dev.

Вместо этого им следует вызвать команду сборки, предоставляемую вашей платформой.

Если вы не уверены, какое значение следует указать, отключите параметр «Переопределение» , чтобы по умолчанию использовались предпочтительные настройки для выбранного вами пресета Framework Preset .

Движок Pnpm не поддерживается
ERR_PNPM_UNSUPPORTED_ENGINEЭто происходит, когда package.json#engines.pnpmне соответствует текущей запущенной версии pnpm.

Для устранения проблемы выполните одно из следующих действий:

Установите значение переменной окружения ENABLE_EXPERIMENTAL_COREPACKравным 1 и убедитесь, что packageManagerэто значение правильно задано в вашем файле конфигурации.package.json
package.json
{
  "engines": {
    "pnpm": "^7.5.1"
  },
  "packageManager": "pnpm@7.5.1"
}
Удалите engines.pnpmзначение из вашегоpackage.json
Вы не можете использовать это engine-strictдля решения данной ошибки. engine-strictОбрабатывает только зависимости.

Функция Yarn dynamic require "util" не поддерживается.
Эта ошибка возникает, когда в проектах используются yarn, corepack и есть package.jsonполе, typeустановленное в значение module. Это известная проблема yarn .

Чтобы предотвратить эту ошибку, рассмотрите следующие варианты:

Удалить "type": "module"из проектаpackage.json
Установите yarn в проект вместо использования corepack.yarn set version [desired-version] --yarn-path
Неверная строка подключения Edge Config
Эта ошибка возникает при попытке создать развертывание, в котором хотя бы одна из переменных среды содержит устаревшую строку подключения Edge Config. Строка подключения может быть устаревшей, если сам файл Edge Config был удален или если токен, используемый в строке подключения, недействителен или был удален.

Для устранения этой ошибки удалите или обновите переменную среды, содержащую строку подключения. В большинстве случаев переменная среды называется EDGE_CONFIG.

Установленные по всему миру @vercel/speed-insightsпакеты@vercel/analytics
Необходимо указать ссылки @vercel/speed-insightsна эти @vercel/analyticsпакеты в файле вашего приложения package.json. Эта ошибка возникает, когда вы развертываете приложение с этими пакетами, доступными глобально, но не указанными в вашем package.jsonфайле, например, в монорепозитории.

Чтобы исправить эту ошибку, добавьте пакеты в свой package.jsonфайл в качестве зависимостей.

Увеличенная страница с инкрементальной статической регенерацией
Ответы Incremental Static Regeneration (ISR) размером более 20 МБ приводят к тому, что страницы не отображаются в продакшене с FALLBACK_BODY_TOO_LARGEошибкой. Это затрагивает все этапы предварительной отрисовки во время сборки Next.js, а также другие фреймворки, использующие функции предварительной отрисовки
